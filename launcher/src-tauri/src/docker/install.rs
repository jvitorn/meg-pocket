//! Docker installation and repair helpers live in `docker::mod` for now.
//!
//! TODO(portable-runtime): move Docker install/repair routines here after the
//! launcher has a release with the runtime dispatcher in place.
