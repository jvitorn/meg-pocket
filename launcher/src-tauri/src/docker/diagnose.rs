//! Docker diagnostics live in `docker::mod` for now.
//!
//! TODO(portable-runtime): split the large Docker backend into this module once
//! the portable runtime is stable enough to avoid changing two launch paths at
//! the same time.
